scrape_configs:
  - job_name: "listmonk"
    scrape_interval: 60s
    scrape_timeout: 10s
    static_configs:
      - targets: ["localhost:8000"]
        labels:
          environment: "production"
          service: "listmonk-exporter"
